import styled from 'styled-components'

const TitleListing = styled.Text`
  font-size: 22px;
  font-weight: bold;
  margin: 10px;
`

const Title = styled.Text`
  font-size: 22px;
  font-weight: bold;
  margin: 10px 0;
`

const CommonText = styled.Text`
`


export { TitleListing, Title, CommonText }